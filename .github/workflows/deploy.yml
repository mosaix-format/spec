name: Deploy to mosaixformat.org

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify deploy
        run: |
          curl -fsSL "${{ secrets.COOLIFY_DEPLOY_WEBHOOK }}"
