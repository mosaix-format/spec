# Raw Data Export

This file has no YAML frontmatter. It lives inside the `exports/` payload folder,
which is declared in the meta note via `payload: [exports/]`.

The checker must skip this file entirely, producing zero errors.

| id | value |
|----|-------|
| 1  | foo   |
| 2  | bar   |
